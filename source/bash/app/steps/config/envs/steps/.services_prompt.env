# ============================================================
# STEP ID: service_prompt
# DESCRIPTION: configuration for servcoe_promt
# ============================================================

# service_promt dialog height
SERVICES_PROMPT_DIALOG_HEIGHT=13

# 'Add Services' button label
SERVICES_PROMPT_EXTRA_LABEL="Add Services"

# `No` label
SERVICES_PROMPT_NO_LABEL="No, Im Good"

# `Go Back` labrel
SERVICES_PROMPT_YES_LABEL="Back"

# yes/no confirmation question
SERVICES_PROMPT_CONFIRMATION_LABEL="Would you like to add some of them?"

# menu title
SERVICES_PROMPT_MENU_TITLE="THE NEXT STEP"

# dialog window and meta data configuration
SERVICES_PROMPT_TOTAL_MESSAGE="
    \nThere are few services that are nice to have:\n
        - ${COLOR_YELLOW}${COLOR_BOLD}mailpit${COLOR_NORMAL}\n
        - ${COLOR_YELLOW}${COLOR_BOLD}rabbitmq${COLOR_NORMAL}\n
        - ${COLOR_YELLOW}${COLOR_BOLD}phpmyadmin${COLOR_NORMAL}\n
        - ${COLOR_YELLOW}${COLOR_BOLD}etc${COLOR_NORMAL}\n\n

Would you like to add some of them?\n\n
"

# dialog window and meta data configuration for gum
SERVICES_PROMPT_TOTAL_MESSAGE_GUM='
   {{ " " }}
   There are few services that are nice to have:
     - {{ Bold (Color "11" "mailpit") }}
     - {{ Bold (Color "11" "rabbitmq") }}
     - {{ Bold (Color "11" "phpmyadmin$") }}
     - {{ Bold (Color "11" "etc") }}
   {{ " " }}
 {{ Color "11" "============================================================== " }}
   {{ " " }}
   {{ " " }}
'